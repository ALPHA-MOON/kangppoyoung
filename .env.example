# DB 자격증명 (docker-compose 가 backend·mysql 에 주입)
DB_USERNAME=policyfund
DB_PASSWORD=change-me
DB_ROOT_PASSWORD=change-me-root

# OpenAI (P3 부터 사용 — 지금은 미설정이어도 됨)
OPENAI_API_KEY=

# nginx 호스트 포트(기본 80). 호스트 80이 사용 중이면 변경.
NGINX_PORT=80
